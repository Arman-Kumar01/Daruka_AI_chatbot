# Darukaa.Earth AI Biodiversity Intelligence Platform Configuration

# API Keys (Optional: system runs in offline/local deterministic mode without them)
GEMINI_API_KEY=
OPENAI_API_KEY=

# LLM Configuration
LLM_PROVIDER=auto # Options: auto, gemini, openai, local
DEFAULT_MODEL=gemini-1.5-flash

# Server Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=true

# RAG & Knowledge Base
CHROMA_PERSIST_DIR=./data/chroma_db
